{
  "name": "windowforlife",
  "private": true,
  "workspaces": [
    "packages/*",
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n front,back -c cyan,yellow \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=frontend && npm run build --workspace=backend",
    "build:prod": "npm run build --workspace=packages/shared && npm run build:frontend && npm run build:backend",
    "build:shared": "npm run build --workspace=packages/shared",
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "npm run build --workspace=backend"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
